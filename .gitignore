# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
app/build/
build/
*.apk
*.aab
*.ap_
*.dex
local.properties

# Keystore & signing secrets — NEVER commit
*.jks
*.keystore
keystore.properties
release.jks

# Kotlin
.kotlin/

# Coverage / reports
/kover/
**/reports/
backend.properties

# Secrets. The backend README tells you to create backend/.env (it holds FLIPPER_API_KEY); make sure a
# local copy can never be committed. .env.example is the tracked template and stays.
.env
**/.env
!**/.env.example

# Playwright MCP scratch output
.playwright-mcp/
